# ========================================
# 8. Payload 만들기
# ========================================

# 문자열 반복
payload = b"A" * 100


# 주소 추가
payload += p32(0x80485b9)


# 여러 값을 한 번에 배치
payload = flat(
    b"A" * 100,
    p32(0x80485b9)
)

ex)
payload = b"A" * offset
payload += p32(read_flag)
